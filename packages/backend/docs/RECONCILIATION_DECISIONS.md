# RECONCILIATION_DECISIONS.md — Dossier d'arbitrage C (branche divergente → main)

> Généré par audit read-only (workflow 27 agents) le 2026-07-14. Branche canonique :
> `feat/wesley-control-on-main` (option A livrée). Branche divergente archivée :
> `feat/external-wiring-fallbacks-2026-07`. **Chaque GO est feature par feature, humain.**

## Légende des verdicts
- **REPLAY_A** : additif, rejouable sur main façon option A (nouveaux fichiers, pas de conflit fiscal). GO = feu vert de replay.
- **ARBITRAGE_FISCAL** : double implémentation d'une feature déjà présente sur main en zone NF525 → **décision produit** (quelle implémentation canonique).
- **ABANDON** : obsolète/redondant avec main, ou remplacé par une version main plus riche.

**Synthèse** : 13 REPLAY_A · 8 ARBITRAGE_FISCAL · 6 ABANDON · 14/27 touchent le fiscal NF525.

## Tableau de synthèse (GO feature par feature)

| Module | Verdict | Effort | Risque fiscal | Maturité | Résumé |
|---|---|---|---|---|---|
| `coupon` | **ARBITRAGE_FISCAL** | M | 🔴 OUI | COMPLET | Module de coupons de fidélité Wesley Club : émission (cron) et surtout redemption POS d'un… |
| `pos-session` | **ARBITRAGE_FISCAL** | L | 🔴 OUI | COMPLET | Primitive de session de caisse (γ-model : une session active par (storeId, terminalId), li… |
| `products` | **ARBITRAGE_FISCAL** | L | 🔴 OUI | COMPLET | Référentiel catalogue produits (CRUD, scan EAN, catégories, historique/analytics de prix, … |
| `reports` | **ARBITRAGE_FISCAL** | L | 🔴 OUI | COMPLET | Module de reporting POS/comptable : génère le Z-report journalier (figé, NF525), les daily… |
| `returns` | **ARBITRAGE_FISCAL** | L | 🔴 OUI | COMPLET | Module Retours / Avoirs (credit notes NF525) : création d'avoirs depuis une vente (par lin… |
| `sales` | **ARBITRAGE_FISCAL** | L | 🔴 OUI | COMPLET | Coeur POS/NF525 : création de vente (panier, promotions, remise manuelle + PIN responsable… |
| `sync` | **ARBITRAGE_FISCAL** | M | 🔴 OUI | COMPLET | Sync offline-first POS↔serveur : POST /sync/push (ventes/clients/ajustements stock créés h… |
| `timewin` | **ARBITRAGE_FISCAL** | M | 🔴 OUI | COMPLET | Proxy d'intégration TimeWin24 : la caisse appelle des endpoints locaux (/timewin/*) qui re… |
| `auth` | **REPLAY_A** | M | non | COMPLET | Authentification centrale POS : login PIN/QR/email (local-first + fallback TimeWin24), gén… |
| `backoffice-discounts` | **REPLAY_A** | S | 🔴 OUI | COMPLET | POS-054e — endpoint admin-only `POST /backoffice/discounts/authorize` qui autorise et jour… |
| `comptamax` | **REPLAY_A** | M | 🔴 OUI | COMPLET | Lecteur de pré-comptabilité Comptamax24, en lecture seule et hors-bande. Construit le jour… |
| `customer-visits` | **REPLAY_A** | S | non | COMPLET | Analytique de fréquence de visite client (comptage, intervalle moyen, récence, segment new… |
| `employees` | **REPLAY_A** | S | non | COMPLET | Gestion des employés côté backend : CRUD, PIN (validation format + unicité par magasin, ha… |
| `integration` | **REPLAY_A** | M | 🔴 OUI | COMPLET | Relais de l'outbox transactionnel d'intégration : expose des flux consommateur read-only (… |
| `inventory-scan` | **REPLAY_A** | S | non | COMPLET | Scan de codes-barres pour comptage d'inventaire / réception / ajustement / retour : lookup… |
| `loyalty-card` | **REPLAY_A** | S | non | COMPLET | Carte de fidélité Wesley Club côté mobile : émet/renouvelle un QR signé HMAC à TTL 60s (GE… |
| `mobile-auth` | **REPLAY_A** | S | non | PARTIEL | Auth JWT des clients mobiles Wesley Club (audience `mobile-app`) : register, login, refres… |
| `promotions` | **REPLAY_A** | S | 🔴 OUI | PARTIEL | Module de promotions tenant-scoped : CRUD des règles promo (promo_rules) + calcul des remi… |
| `sales-ai` | **REPLAY_A** | S | non | COMPLET | Moteur de recommandations/upsell « cash-first » basé sur des règles : analyse la co-occurr… |
| `shift-reminders` | **REPLAY_A** | S | non | COMPLET | Cron (toutes les 30 min) qui envoie un rappel SMS/email avant le service d'un employé, à p… |
| `subscriptions` | **REPLAY_A** | S | non | COMPLET | Gère le cycle de vie des abonnements magasin (trial/plans, statut, période) et l'enforceme… |
| `customers` | **ABANDON** | S | non | COMPLET | Référentiel client / fidélité Wesley Club : CRUD client, génération QR de carte, envoi et … |
| `suppliers` | **ABANDON** | M | non | COMPLET | Référentiel fournisseur tenant-scoped (P327, "variantes option A") : CRUD REST /suppliers … |
| `stock` | **ABANDON** | L | 🔴 OUI | COMPLET | Gestion des stocks POS : décrément à la vente, ajustement manuel (delta/absolu, lock pessi… |
| `stock-locations` | **ABANDON** | S | non | COMPLET | Gestion de stock multi-emplacements (central/magasin/transit/perte) : soldes par emplaceme… |
| `stores` | **ABANDON** | S | non | COMPLET | Référentiel magasins multi-tenant : CRUD magasins, hiérarchie org/unité, sync bidirectionn… |
| `stripe-terminal` | **ABANDON** | S | 🔴 OUI | COMPLET | Client Stripe Terminal (paiement carte présent / card-present) : connection-token, Payment… |

## Décisions fiscales requises (ARBITRAGE_FISCAL) — GATED, décision produit

### `coupon`  — effort M · 🔴 fiscal
- **Rôle** : Module de coupons de fidélité Wesley Club : émission (cron) et surtout redemption POS d'un coupon de remise (discount_value %) avec verrou FOR UPDATE, idempotency-key obligatoire, contrôles propriété/statut/expiration/cooldown 15j, marquage USED, insertion de la visite et émission d'une entrée d'audit COUPON_REDEEMED dans la chaîne.
- **Maturité** : COMPLET — controller/module/cron inchangés vs main ; service.redeem fonctionnel (transaction complète : cache idempotence, lock, refus, USED, visite, audit) ; extraction pure coupon-policy.ts (isValidIdempotencyKey, isCouponAvailable, isCouponExpired, cooldownEnd, isInCooldown, daysRemainingInCooldown) ; deux
- **Tests** : 3 suites côté session : coupon-policy.spec.ts (~60 lignes, tous les prédicats purs), coupon.service.pgmem.spec.ts (152 lignes, redemption sur Postgres in-memory : lock FOR UPDATE, rejeu idempotent, refus propriété/statut
- **Dépendances schéma** : AUCUNE. Ne touche que des tables standards déjà sur main : coupons, customer_visits, idempotency_keys, loyalty_reward_cycles, customers. Aucune colonne du schéma branche (pas de variant_label, pos_session_id, price_override, integration_events, suppliers, opening_float...). La dépendance test/helper
- **Recommandation** : Deux parties de nature opposée. (1) Additif et sain : extraction pure coupon-policy.ts + suites policy/pgmem — rejouable façon option A. (2) Régression fiscale : coupon.service.ts REVERT le correctif D16 de main. Sur main l'audit COUPON_REDEEMED est émis APRES commit (best-effort : un échec d'audit ne rollback pas la redemption engagée, et un replay de cache ne ré-audite pas). La session le remet 

### `pos-session`  — effort L · 🔴 fiscal
- **Rôle** : Primitive de session de caisse (γ-model : une session active par (storeId, terminalId), liée au terminal physique via header X-Terminal-Id). Gère ouverture/fermeture de session, fond de caisse à l'ouverture, comptage espèces + écart figé à la clôture, et lookup de la session active du terminal (base du binding vente↔opérateur).
- **Maturité** : COMPLET — Côté branche session le module est cohérent et fini : controller (open/close/cash-summary/active), service complet (openSession avec backstop TOCTOU 23505, closeSession avec calcul d'écart serveur, getSessionCashSummary via SQL brut, findActiveForTerminal), DTO validés (class-validator), 2 helpers p
- **Tests** : 28 tests sur la branche : pos-session.service.spec.ts (14 : invariant γ, refus sans terminalId, conflit 409 double session, mapping 23505, calcul écart clôture, cash-summary), cash-count.spec.ts (8 : math de réconciliati
- **Dépendances schéma** : DÉPEND de colonnes de schéma BRANCHE qui DIFFÈRENT de main. Sur main (entité + migrations 1735+) : opening_cash_minor_units, opening_cash_set_at, opening_cash_corrected_by/at, cash_sales_minor_units, cash_refunds_minor_units, expected_cash_minor_units, counted_cash_minor_units, cash_difference_minor
- **Recommandation** : Double implémentation de la MÊME feature fiscale (session de caisse, fond d'ouverture, comptage espèces, écart, source Z-report) déjà présente et plus riche sur main. Divergences non additives : (1) noms de colonnes différents pour le même concept (opening_float vs opening_cash, cash_variance vs cash_difference) → collision de schéma, pas rejouable ; (2) câblages opposés — la branche route vers l'

### `products`  — effort L · 🔴 fiscal
- **Rôle** : Référentiel catalogue produits (CRUD, scan EAN, catégories, historique/analytics de prix, alertes stock, import catalogue en masse, génération code-barres EAN-13). Sur la branche session, réécriture importante avec helpers purs (résolution prix effectif, verdict marge/jour, normalisation nom anti-doublon, validation import, synthèse catalogue).
- **Maturité** : COMPLET — Routes + service + 7 helpers purs cohérents et entièrement câblés (controller expose create/import/findAll/scan/categories/catalog-summary/stock-alerts/price-history/price-analytics/generate-barcode). AuditService branché (append-only) sur create/update/delete/import. 63 cas de test répartis sur 9 f
- **Tests** : Bien couvert : ~63 it() sur 9 specs. ean13 (9), price-verdict (7), product-analytics (7), name-normalize (6), import-catalog (5), catalog-summary (4), price-resolve (4), products.service (7 unitaires), products.service.p
- **Dépendances schéma** : CONFLIT direct avec le schéma de main (les deux branches divergent sur product.entity.ts). Session dépend de : products.price_override_minor_units (colonne inline, mig 1723) ALORS QUE main utilise une TABLE séparée store_product_prices (store-product-price.entity.ts, absente en session) ; products.v
- **Recommandation** : Double implémentation des mêmes features (prix override par magasin, variantes, marque, fournisseurs) déjà présentes sur main mais avec un schéma INCOMPATIBLE : override en colonne inline vs table store_product_prices, variant_label vs variant_name, brand varchar vs brand_id uuid, suppression de sku/status. Non rejouable additivement — un replay détruirait le modèle catalogue de main et casserait 

### `reports`  — effort L · 🔴 fiscal
- **Rôle** : Module de reporting POS/comptable : génère le Z-report journalier (figé, NF525), les daily-summary, KPI magasin, analytics produits/tendances, et — côté branche — ajoute ventes-par-employé (POS-094), export comptable local CSV/JSON depuis le Z figé (POS-100), ventilation des paiements par mode (POS-102), agrégateurs purs (z-report-aggregate POS-122, average-basket). La clôture de caisse émet en plus un événement outbox `cash_session.closed` transactionnellement.
- **Maturité** : COMPLET — Routes câblées dans reports.controller.ts (sales-by-employee, accounting-export, payments-breakdown), service implémenté avec agrégateurs purs extraits, 12 fichiers spec. Seul l'envoi externe vers Comptamax24 est explicitement non implémenté (commentaire « Sending to Comptamax24 is NOT implemented »
- **Tests** : 12 fichiers spec sur la branche : z-report-aggregate.spec, accounting-export.spec, average-basket.spec, cash-session-events.spec, payments-breakdown.spec, sales-by-employee.spec, product-analytics.report/trend/util/pgmem
- **Dépendances schéma** : integration_events (IntegrationEventEntity) — la branche importe IntegrationEventEntity + IntegrationModule + StoreOrgResolver et fait `m.insert(IntegrationEventEntity, [toOutboxRow(event)])` dans la transaction de generateZReport. Ni l'entité integration-event.entity.ts ni la migration outbox (1725
- **Recommandation** : Zone fiscale NF525 pleine (Z-report, export comptable, clôture caisse, ventilation paiements) et DIVERGENT : le module réécrit generateZReport — déjà présent sur main — pour y greffer un événement outbox transactionnel `cash_session.closed` qui dépend de la table integration_events absente de main (migration 1725 gated). Il SUPPRIME de surcroît deux endpoints existants sur main (x-report, /summary

### `returns`  — effort L · 🔴 fiscal
- **Rôle** : Module Retours / Avoirs (credit notes NF525) : création d'avoirs depuis une vente (par line-item ou par ticket EAN pour le sync offline), remboursement cash/carte/store_credit, émission de cartes cadeaux, restitution de stock, chaîne de hash par magasin, idempotence, et redemption store-credit. Zone strictement fiscale (avoirs, chaîne d'audit, TVA).
- **Maturité** : COMPLET — Service, controller, module et 4 helpers (credit-code, refund-events, refund-policy, returns-policy) cohérents et câblés ; 6 fichiers de specs (~49 tests) ; chaîne de hash, idempotence, retry sérialisé (FOR UPDATE stores), audit append-only, restitution stock et outbox tous implémentés bout-en-bout.
- **Tests** : 6 specs, ~49 tests unitaires : returns.service.spec (18 : flux avoir, idempotence, quantités retournables, gift card), returns-policy.spec (11 : arrondi cumulatif POS-INT-127, returnableQuantity), refund-policy.spec (6 :
- **Dépendances schéma** : DÉPEND de integration_events (migration 1725000000000-AddIntegrationOutbox) — table ABSENTE de main (main n'a ni l'entité integration-event, ni cette migration, ni le module integration/StoreOrgResolver). Utilise IntegrationEventEntity, toOutboxRow, StoreOrgResolver (POS-INT-89). Inversement, la ver
- **Recommandation** : Double implémentation d'une même feature fiscale (avoirs/credit notes NF525) sur deux lignées divergentes. main scelle le fiscal_journal dans la même transaction, lie l'avoir à la pos_session et alimente employee-score ; la branche session a SUPPRIMÉ ce scellement fiscal et ce binding, et branché à la place un outbox integration_events + StoreOrgResolver + ventilation TVA + journal de mouvements s

### `sales`  — effort L · 🔴 fiscal
- **Rôle** : Coeur POS/NF525 : création de vente (panier, promotions, remise manuelle + PIN responsable), paiements multi-moyens dont avoir/store-credit, chaîne de hachage SHA-256, journal fiscal, décrément de stock, points de fidélité, historique des ventes. C'est la zone fiscale centrale de la caisse.
- **Maturité** : COMPLET — Flux entièrement câblé : service 1478 lignes (routes controller + service + 15 fichiers spec cohérents), chaîne de hachage (hashChainPrev/Current, prevHash, sha256), FiscalJournalEntity, AuditService.log, redemption avoir/store-credit atomique. Helpers purs extraits et testés (tax, sale-total, disco
- **Tests** : 15 fichiers spec dans le module : discount-policy(+edge), discount-totals, tax, sale-total, payment-policy, pin-attempt-limiter, loyalty-points, ticket-number, sale-events, list-sales-query.dto, sales-history-filters.pgm
- **Dépendances schéma** : DÉPENDANCES DIVERGENTES du schéma branche : (1) sale.posSessionId -> colonne pos_session_id (migr. 1726) ALORS QUE main mappe le MÊME concept sur sessionId -> colonne session_id (conflit de nom sur un binding fiscal) ; (2) IntegrationEventEntity / table integration_events (outbox, migr. 1725) + buil
- **Recommandation** : Double implémentation incompatible du flux de vente NF525 : les deux lignées ont réécrit le service (~1500-1700 lignes) dans des directions divergentes. Conflit direct de schéma sur un binding fiscal (session_id sur main vs pos_session_id sur session), plus outbox intégration + price-resolve côté session absents de main, et gate enrollment + promo-codes + Stripe + routes pending-payments/regulariz

### `sync`  — effort M · 🔴 fiscal
- **Rôle** : Sync offline-first POS↔serveur : POST /sync/push (ventes/clients/ajustements stock créés hors-ligne, dédup batch par id client + résolution de conflits), GET /sync/pull (produits/clients modifiés depuis lastSync) et GET /sync/status. Tenant-scopé, écrit via AuditService.
- **Maturité** : COMPLET — Routes + service + module cohérents ; logique de conflit extraite en module pur (conflict.ts) ; 3 fichiers de specs dont un pg-mem sur Postgres réel prouvant l'invariant de rejeu offline. Aucun TODO/stub. Câblé dans SyncModule (identique à main).
- **Tests** : 3 specs. conflict.spec.ts (~66 l) : unitaires partitionPushSales (idempotence id-less), resolveCustomerSync (server-wins), isServerNewerThanSync. sync.service.pgmem.spec.ts (148 l) : pg-mem réel — rejeu d'un payload ne d
- **Dépendances schéma** : Importe recordAdjustMovement de ../stock/stock-movement-journal.ts — FICHIER SESSION-ONLY absent de main (divergence stock P306) ; il écrit dans stock_movements/stock_locations (tables présentes sur main via migration 1735, mais pas le helper). Divergence aussi sur sale.entity.session_id/terminal_id
- **Recommandation** : Double implémentation divergente du flux de sync fiscal (rejeu vente offline NF525, chaîne audit). La branche apporte du bon (conflict.ts pur testé, rejet des ventes sans id = idempotence POS-INT-136, journal stock append-only sur ajustements), MAIS son push() RETIRE le durcissement M403/D5 présent sur main : forçage serveur de storeId + session_id/terminal_id null sur chaque vente synchronisée (a

### `timewin`  — effort M · 🔴 fiscal
- **Rôle** : Proxy d'intégration TimeWin24 : la caisse appelle des endpoints locaux (/timewin/*) qui relaient vers TW24 — login employé, sync employés + cache offline, shifts du jour, paie mensuelle, horaires/config magasin, pointage clock-in/out, feed magasins, et webhook d'événements sortants (sale.completed, session.opened/closed, stock.alert). Circuit breaker + HMAC POS. La branche session ajoute en plus des helpers purs de réconciliation présence POS↔TW24 et d'agrégation d'amplitude de shifts, consommés par le module integration/outbox.
- **Maturité** : COMPLET — Service + controller + module complets et testés des deux côtés. Session : 7 specs (~53 tests) — pos-hmac 5, health-status 2, employee-map 3, shift-adapter 8, shift-amplitude 9, presence-reconciliation 8, timewin.controller 18. Les nouveaux helpers sont réellement câblés (importés par integration/re
- **Tests** : Session : 7 fichiers de specs, ~53 cas (pos-hmac 5, health-status 2, employee-map 3, shift-adapter 8, shift-amplitude 9, presence-reconciliation 8, timewin.controller 18). Main possède EN PLUS un spec absent de la sessio
- **Dépendances schéma** : Deux dépendances de schéma branche. (1) Côté régression : le pushEvent de MAIN dépend de store.tw24_enabled (StoreEntity) et de la table timewin_event (TimewinEventEntity) ; la session a RETIRÉ ces deux dépendances et son store.entity ne porte pas tw24Enabled — la session est en retard sur main ici.
- **Recommandation** : Divergence bidirectionnelle, pas un simple additif rejouable. La session est une double implémentation de pushEvent TW24 qui, par rapport à main, RÉGRESSE deux garanties déjà présentes sur main en zone sensible : (a) l'idempotence des événements TW24 (claim-before-send sur idempotency_key, décision « pas de doublon ») et (b) le toggle TW24 par magasin (store.tw24_enabled). Ces événements portent s

## Rejouables additivement (REPLAY_A) — GO = feu vert de replay

### `auth`  — effort M
- **Rôle** : Authentification centrale POS : login PIN/QR/email (local-first + fallback TimeWin24), génération JWT access/refresh, validation d'employé, logout/révocation. La branche session y ajoute P370 : passkeys WebAuthn/FIDO2 (Face ID/Touch ID/Windows Hello/clé FIDO2) avec enregistrement, login découvrable, et gestion des clés (liste/renommer/révoquer).
- **Maturité** : COMPLET — Feature WebAuthn complète côté logiciel : service (317 l.), controller (6 routes throttlées, register sous JWT / login public), entité + migration 1759 auto-suffisante, dépendance @simplewebauthn/server déclarée, helper createSessionForEmployee extrait proprement. Tests présents et cohérents. Commit
- **Tests** : 25 tests dans le module : webauthn.service.spec.ts (14 — enregistrement, login, anti-rejeu challenge, anomalie compteur, filtrage par compte), auth.service.spec.ts (8), upstream-status.spec.ts (3 — classification 5xx/abs
- **Dépendances schéma** : Introduit une table PROPRE et NOUVELLE : webauthn_credentials (entité webauthn-credential.entity.ts + migration 1759000000000, absente de main). Table auto-suffisante : CREATE TABLE + uuid-ossp uniquement, ne dépend que de employees.id (présent sur main). AUCUNE dépendance aux colonnes/tables diverg
- **Recommandation** : La contribution réelle de la branche (P370 passkeys) est purement additive et rejouable : nouveaux fichiers webauthn.*, upstream-status.ts, entité + migration 1759 auto-suffisante, dép @simplewebauthn, plus l'extraction de createSessionForEmployee. ATTENTION : le diff origin/main→session affiche de fausses « suppressions » (lockout brute-force par IP, re-check DB dans validateEmployee, addSelect p

### `backoffice-discounts`  — effort S · 🔴 fiscal
- **Rôle** : POS-054e — endpoint admin-only `POST /backoffice/discounts/authorize` qui autorise et journalise une remise back-office (≤100%, motif+audit obligatoires au-delà de 30%), strictement séparé du chemin caisse (terminal POS plafonné en dur à 30%). Il valide la décision et l'inscrit dans la chaîne d'audit ; il n'applique PAS encore la remise à une vente/ticket (suite POS-054e-apply volontairement hors périmètre).
- **Maturité** : COMPLET — 5 fichiers cohérents (controller + service + module + DTO class-validator + spec), câblé dans app.module.ts (import ligne 53, déclaration ligne 143). Garde JwtAuthGuard+RolesGuard @Roles('admin') + re-vérification par la policy (défense en profondeur). Logique déléguée à evaluateManualDiscount(chann
- **Tests** : 1 spec unitaire (backoffice-discount.service.spec.ts), 5 tests par instanciation directe avec AuditService mocké : admin 50% + motif = OK/audité 'authorized', admin 100% + motif = OK, admin >30% sans motif = BadRequest +
- **Dépendances schéma** : AUCUNE colonne/table du schéma branche divergente : le module ne touche aucune entité (pas de variant_label, price_override, pos_session_id, integration_events, suppliers...). Il n'écrit qu'via AuditService.log (présent sur main). ATTENTION dépendance de CODE, pas de schéma : il importe `../sales/di
- **Recommandation** : Feature nette additive absente de main (module + canal back-office ≤100%), sans changement de schéma DB, rejouable sur main sans conflit façon option A. Pas ARBITRAGE_FISCAL : la remise back-office ≤100% n'existe pas sur main (seul un cap inline 30% côté caisse y figure), donc pas de double implémentation d'une même feature fiscale — les deux concernent des canaux distincts. Condition de replay : 

### `comptamax`  — effort M · 🔴 fiscal
- **Rôle** : Lecteur de pré-comptabilité Comptamax24, en lecture seule et hors-bande. Construit le journal comptable en partie double (plan PCG français) sur une journée ou une période à partir de l'outbox d'intégration (sale.completed, sale.voided/annulation, payment.captured, refund.created, credit_note.issued), plus un contrôle de caisse/écart (rapprochement paiements capturés vs totaux figés du Z-report par tender) et un export social TimeWin→Comptamax (justificatif RH, PAS d'écritures sociales réelles). 3 routes GET (journal, cash-control, social), tenant-scopé via req.user.storeId (anti-IDOR), ne touche jamais le chemin caisse.
- **Maturité** : COMPLET — Controller (3 endpoints admin/manager json+csv) + service + 6 modules-métier purs (pre-accounting, cash-control, journal-range, payroll-adapter, social-preaccounting, social-entries-guard, account-map). Cohérence routes↔service↔helpers vérifiée. Conception fail-closed explicite : social-entries-guar
- **Tests** : 9 fichiers de specs colocalisés, ~61 tests : pre-accounting.spec (13), cash-control.spec (9), comptamax.controller.spec (8), social-entries-guard.spec (7), account-map.spec (6), social-preaccounting.spec (6), journal-ran
- **Dépendances schéma** : Dépendance forte au SCHÉMA BRANCHE : table integration_events (IntegrationEventEntity : type/aggregate_type/aggregate_id/store_id/occurred_at/payload jsonb/status...) — ENTIÈREMENT absente de main, créée par la migration 1725000000000-AddIntegrationOutbox (GATE2, NON exécutée sur la DB cible). Co-dé
- **Recommandation** : Additif et rejouable sur main : aucun équivalent (comptamax/journal/cash-control/pré-compta) n'existe sur main, donc pas de double implémentation ni de conflit — ce n'est PAS un ARBITRAGE_FISCAL. Code strictement lecture seule, hors-bande, ne modifie jamais ventes/Z-report/chaîne d'audit (il ne fait que les LIRE via l'outbox), avec garde fail-closed sur les écritures sociales. fiscalRisk=true car 

### `customer-visits`  — effort S
- **Rôle** : Analytique de fréquence de visite client (comptage, intervalle moyen, récence, segment new/regular/occasional/at_risk) exposée en lecture seule via GET /customer-visits/:customerId/frequency. Aucune écriture, aucune donnée monétaire.
- **Maturité** : COMPLET — Sur main le module n'avait que module.ts + service.ts (pas de route). La branche ajoute additivement : controller câblé (JwtAuthGuard + RolesGuard manager/admin + ParseUUIDPipe), méthodes service getFrequency/getFrequencySecured, deux helpers purs (visit-frequency.ts calcul déterministe, customer-ac
- **Tests** : 4 fichiers de specs (tous nouveaux/additifs) : customer-access.spec.ts (RBAC/anti-IDOR admin/même magasin/refus cross-store/fail-closed), visit-frequency.spec.ts (segments, cas 0/1/N visites), customer-visits.service.spe
- **Dépendances schéma** : AUCUNE. N'utilise que des colonnes déjà présentes sur main : customer_visit.visitedAt / customerId / storeId et customer.storeId (varchar nullable). Aucune dépendance aux colonnes du schéma branche (variant_label, price_override, pos_session_id, opening_float, integration_events, suppliers...).
- **Recommandation** : Diff 100% additif au-dessus d'origin/main (444+/2- réparties sur controller neuf, helpers purs neufs, specs neuves, +2 lignes d'enregistrement module et méthodes service ajoutées, sans réécrire l'existant). Aucune divergence de schéma, aucune zone fiscale NF525 (analytique de visites en lecture seule, pas de ventes/avoirs/reçus/session caisse/Z-report/chaîne audit). Rejouable sur main sans conflit

### `employees`  — effort S
- **Rôle** : Gestion des employés côté backend : CRUD, PIN (validation format + unicité par magasin, hash bcrypt), badge QR, droits/rôles par défaut (admin/manager/cashier), activation/désactivation. Le PIN sert à l'authentification caisse.
- **Maturité** : COMPLET — Module déjà présent et complet sur main (routes+service+specs cohérents). La divergence session est minime (5 fichiers) : extraction pin-policy pure bien testée, refacto de la requête d'unicité, et retrait d'un décorateur @Roles. Complet fonctionnellement mais la divergence introduit une régression 
- **Tests** : 2 specs. employees.service.spec.ts (unicité PIN par magasin + validation format) — modifié sur la branche pour mocker find() au lieu du QueryBuilder ; il reste vert mais masque la régression addSelect. pin-policy.spec.ts
- **Dépendances schéma** : AUCUNE. Aucune colonne/table du schéma branche (pas de variant_label, pos_session_id, opening_float, integration_events, suppliers). Entité employee standard partagée avec main. NB : pinHash est select:false — le refacto find({where}) perd le .addSelect('e.pinHash') du QueryBuilder d'origine, donc p
- **Recommandation** : Divergence additive et rejouable sans conflit de schéma (AUCUNE dépendance schéma branche). L'extraction pin-policy.ts (pur, testé) est un gain net à conserver. Deux points à corriger AVANT replay : (1) le refacto assertPinUniqueInStore passe de QueryBuilder+addSelect('e.pinHash') à find({where}) et perd la sélection de pinHash (select:false) → l'unicité PIN par magasin devient inopérante, régress

### `integration`  — effort M · 🔴 fiscal
- **Rôle** : Relais de l'outbox transactionnel d'intégration : expose des flux consommateur read-only (events incrémental keyset, reconciliation POS↔TimeWin, shifts/amplitude caisse, stock-signals, stats outbox) et flushe l'outbox hors bande vers Comptamax24/TimeWin24/Analytik R via un publisher abstrait (sink simulation par défaut, sink HTTP HMAC gated par env). Ne bloque jamais la caisse.
- **Maturité** : COMPLET — 25 fichiers, contrôleur à 6 routes (5 GET read-only rôles admin/manager + POST /relay admin-only), 4 services (relay, query, reconciliation, resolver), cron de relais, abstraction publisher (Simulation/Http + factory env fail-closed sans secret), 12 specs dont un e2e loopback pgmem (relay-e2e-loopba
- **Tests** : 12 fichiers de specs couvrant : contrat consommateur, pagination keyset, requête events, contrôleur, publisher (simulation + HTTP), query service pgmem, relay service, stats, publish-request (HMAC/enveloppe), reconciliat
- **Dépendances schéma** : Dépend de la table NEUVE integration_events (migration 1725000000000-AddIntegrationOutbox, ABSENTE de main — main est déjà à 1735-1758) + entité integration-event.entity.ts absente de main + helpers common/integration/* absents de main. reconciliation.service lit pos_sessions (colonnes standard stor
- **Recommandation** : Module 100% additif et non-conflictuel : main n'a AUCUN outbox/intégration, donc pas de double implémentation à arbitrer. Nouvelle table + fichiers neufs, rejouable façon option A. Read-only côté consommateur, relais hors-bande jamais dans le chemin caisse, sink simulation fail-closed sans secret, append-only respecté (NF525). fiscalRisk=true car le flux véhicule des events ventes/avoirs/cash_sess

### `inventory-scan`  — effort S
- **Rôle** : Scan de codes-barres pour comptage d'inventaire / réception / ajustement / retour : lookup produit tenant-scoped par EAN, rejeu idempotent via clientEntryId, puis application atomique aux stocks (product.stock_quantity) en mode ABSOLUTE (inventaire = le comptage physique remplace) ou DELTA (réception/ajustement/retour). La divergence session ajoute (a) un helper pur extrait applyStockAdjustment/inventoryVariance et (b) l'émission d'un mouvement append-only dans stock_movements à chaque ajustement, dans la même transaction (P306, option 1).
- **Maturité** : COMPLET — Controller + service + module cohérents, déjà présents sur main. La divergence (5 fichiers) est un refactor additif propre : extraction d'un helper pur (comportement préservé, Math.max clamp identique à l'ancien inline) + un appel recordAdjustMovement(deltaQuantity = après−avant ; 0 = aucun mouvemen
- **Tests** : Solide, 4 fichiers de specs. Ajouts session : inventory-adjust.spec.ts (7 tests unitaires du helper pur : delta additif, clamp 0, absolute remplace, variance surplus/manquant/exact) ; inventory-scan.service.pgmem.spec.ts
- **Dépendances schéma** : AUCUNE colonne de schéma-branche propre au module. Les tables product/sale/pos_session ne sont pas modifiées. L'émission de journal écrit dans stock_movements / stock_locations via StockMovementEntity/StockLocationEntity — ces tables et entités EXISTENT DÉJÀ sur main (migration 1735000000000-CreateS
- **Recommandation** : Divergence 100% additive et behavior-preserving : extraction d'un helper pur + écriture d'un mouvement append-only stock_movements dans la transaction d'ajustement. Aucune migration propre au module requise (table stock_movements déjà sur main via 1735). Rejouable sur main sans conflit à condition d'embarquer le helper partagé stock/stock-movement-journal.ts (même lot external-wiring, lui aussi ad

### `loyalty-card`  — effort S
- **Rôle** : Carte de fidélité Wesley Club côté mobile : émet/renouvelle un QR signé HMAC à TTL 60s (GET /mobile/loyalty-card, POST regenerate-qr), résout le token au scan POS, suspend/rotate la carte. Lecture des coupons via CouponService. Ne crée ni vente ni avoir.
- **Maturité** : COMPLET — Routes câblées (LoyaltyCardController sous MobileAuthGuard), service complet, 27 tests cohérents sur 3 specs, helpers documentés et couverts. Refactor propre sans TODO ni stub.
- **Tests** : Oui, riche. 3 fichiers de specs sur la branche : qr-token.spec.ts (8 cas, helpers purs TTL/expiry/constant-time/claims/card-active), loyalty-card.service.spec.ts (12 cas, NOUVEAU — createCard/getCardView/rotate/suspend/r
- **Dépendances schéma** : AUCUNE. Le module ne dépend que de LoyaltyCardEntity et CouponEntity, tous deux identiques sur main (aucun diff entity). Aucune référence à variant_label, price_override, pos_session_id, integration_events, suppliers ou autre colonne du schéma branche.
- **Recommandation** : Divergence = refactor pur, préservant le comportement : extraction de 5 helpers QR (TTL, isTokenExpired avec le même '>' strict, constantTimeEqual, hasRequiredClaims, isCardActive) dans un nouveau qr-token.ts, plus rewiring des deux services et ajout/maj de specs. Controller, module et entities inchangés (diff vide). Additif, sans conflit attendu sur main, façon option A. Zone fidélité/coupons uni

### `mobile-auth`  — effort S
- **Rôle** : Auth JWT des clients mobiles Wesley Club (audience `mobile-app`) : register, login, refresh, getMe/updateMe, soft-delete RGPD. La divergence extrait la fabrication des tokens (access 15m / refresh 30d, anti-duplication `aud`) dans un helper pur `mobile-tokens.ts` et ajoute deux specs.
- **Maturité** : PARTIEL — Le coeur auth (5 routes + service + guards) est complet et câblé. Mais la divergence introduit (a) une régression sécurité — voir schemaDeps — et (b) une sur-affirmation RGPD : le commentaire du controller passe de la version honnête de main (« soft-delete uniquement, anonymisation M302 gatée OFF »)
- **Tests** : 2 specs ajoutées (~10 tests). mobile-tokens.spec.ts (5) : pas de duplication `aud`, contenu access (sub/email/aud), refresh signé avec un secret distinct et sans email, rejet mauvaise audience, TTL 15m/30j. mobile-auth.s
- **Dépendances schéma** : Zone variantes/prix/pos_session/integration_events : AUCUNE. MAIS divergence sur une entité partagée : customer.entity.ts retire `select:false` de `password_hash` (main le garde `select:false`). Le changement de login couplé (queryBuilder `.addSelect('c.passwordHash')` → `findOne`) NE FONCTIONNE que
- **Recommandation** : Le vrai apport — helper pur `mobile-tokens.ts` + les 2 specs (token builder et gardes register/login) — est purement additif et rejouable sur main sans conflit (pattern « pilotage »). À rejouer en excluant explicitement deux régressions : (1) le retrait de `select:false` sur customer.passwordHash + le passage login queryBuilder→findOne (régression sécurité couplée à une entité partagée), à laisser

### `promotions`  — effort S · 🔴 fiscal
- **Rôle** : Module de promotions tenant-scoped : CRUD des règles promo (promo_rules) + calcul des remises applicables à un panier (applyPromos) pour les types buy_x_get_discount, percentage et first_purchase. Sert de moteur de suggestion de remise en amont de la vente.
- **Maturité** : PARTIEL — Le module existe déjà sur main (COMPLET) ; la branche apporte POS-073 (plafond d'usage) + refactor. Le refactor est propre et testé, MAIS la feature phare POS-073 est à moitié câblée : getActivePromos exclut bien les promos au plafond (filtre SQL sur usage_limit/usage_count), or RIEN n'incrémente us
- **Tests** : 3 fichiers de specs (pures, sans DB) : promo-discount.spec.ts (7 tests : math buy_x_get/percentage/first_purchase, arrondis centimes), promo-policy.spec.ts (11 tests : fenêtre active, isUsageLimitReached null=illimité, d
- **Dépendances schéma** : DÉPEND de 2 colonnes du schéma branche absentes sur main : promo_rules.usage_limit (integer nullable) et promo_rules.usage_count (integer default 0), ajoutées par la migration 1724000000000-AddPromoUsageLimit (absente de main, NON encore jouée sur la DB cible — GATE2). La requête getActivePromos fil
- **Recommandation** : Rejouable additivement sur main sans conflit : migration 1724 non destructive (colonnes nullable/défaut, réversible), delta entité minimal, extraction de helpers purs behavior-preserving pour les types existants, +32 tests. Le seul changement de comportement (anti-cumul dedupeBestPerProduct + filtre plafond) est protecteur (réduit/limite les remises, NULL=illimité => aucun changement par défaut) e

### `sales-ai`  — effort S
- **Rôle** : Moteur de recommandations/upsell « cash-first » basé sur des règles : analyse la co-occurrence des ventes, la marge, la pression de stock et les patterns horaires pour suggérer des produits, et journalise/mesure la performance des recommandations (CTR/conversion). Enrichissement contexte externe (météo/transport). Lecture seule sur les ventes, aucune écriture fiscale.
- **Maturité** : COMPLET — Le module existe déjà sur main avec controller + 3 services câblés. La branche est un refactor iso-comportement : la logique inline de scoring est extraite dans 4 helpers purs (reco-scoring.ts, weather-impact.ts, upsell-scoring.ts, temporal-pattern.ts), les 3 services délèguent à ces helpers, contro
- **Tests** : Fort et entièrement nouveau : 6 fichiers de specs ajoutés par la branche (aucun sur main), ~45 tests — reco-scoring 6, weather-impact 7, upsell-scoring 12, temporal-pattern 7, ai-learning.service 4, external-context.serv
- **Dépendances schéma** : AUCUNE. git grep sur variant_label/variant_name/price_override/store_product_prices/pos_session_id/session_id/opening_float/opening_cash/integration_events/brand_id/supplier dans le module = 0 résultat. Les services lisent SaleEntity, SaleLineItemEntity, ProductEntity et AiRecommendationLogEntity vi
- **Recommandation** : Refactor purement additif et iso-comportement : extraction de logique dans des helpers purs + ajout de ~45 tests, sans changement de routes, de wiring, ni de schéma DB. Aucune dépendance au schéma de branche, controller/module identiques à main. Rejouable sur main sans conflit de fond (seuls les 3 services existants sont touchés, par substitution 1:1). Pas de zone fiscale NF525 : le module ne fait

### `shift-reminders`  — effort S
- **Rôle** : Cron (toutes les 30 min) qui envoie un rappel SMS/email avant le service d'un employé, à partir du feed TimeWin24 today-shifts. OFF par défaut (SHIFT_REMINDERS_ENABLED + canal configuré requis), dédup en mémoire par jour, dégradation gracieuse si TW24 tombe.
- **Maturité** : COMPLET — Service + cron + module enregistré dans app.module.ts (ligne 128). Pipeline complet : gating isEnabled, normalizeShifts défensif, selectDueShifts pur, sweep avec dédup et fallback TW24. Feature quiet-hours entièrement câblée (isSuppressed appelé en tête de runReminderSweep). Pas de TODO ni stub.
- **Tests** : 2 specs, ~15 tests. quiet-hours.spec.ts (5 : isQuietHour même-jour/wrap-minuit/fenêtre-vide, isHoliday x2). shift-reminder.service.spec.ts (10 : selectDueShifts x2, normalizeShifts x1, gating isEnabled x1, runReminderSwe
- **Dépendances schéma** : AUCUNE colonne/table de schéma. Pas d'entité TypeORM, pas de migration. Dépend uniquement de services externes (TimewinService.fetchStores/getTodayShifts, NotificationService). Attention : dépendance CODE (non-schéma) — la branche session supprime shift-normalize.util.ts que main partage avec pos-se
- **Recommandation** : La seule nouveauté vs main est la suppression quiet-hours/jours fériés : additive, OFF par défaut (fenêtre vide = zéro changement), rejouable proprement. NE PAS faire de replay pleine-page : la branche session ré-inline normalizeShifts et supprime shift-normalize.util.ts, que main a extrait et partage avec pos-session — un replay brut casserait pos-session. Porter uniquement quiet-hours.ts + isSup

### `subscriptions`  — effort S
- **Rôle** : Gère le cycle de vie des abonnements magasin (trial/plans, statut, période) et l'enforcement des quotas (maxProducts/maxEmployees) plus la facturation Stripe (checkout/portail/webhook). Bloque l'accès quand un abonnement est suspendu ou expiré.
- **Maturité** : COMPLET — Module déjà présent et câblé sur main (controller/service/module/stripe-billing identiques). Les 5 fichiers divergents sont additifs : extraction d'un helper pur subscription-policy.ts + refactor behavior-preserving du service (isUnlimited/isWithinLimit/subscriptionAccessDenial) + 3 specs. Aucun TOD
- **Tests** : 3 fichiers de specs AJOUTÉS par la branche (absents sur main) : subscription-policy.spec.ts (limites -1=illimité, within/over, denial suspended/cancelled+grâce/expiré ~8 cas) ; subscriptions.service.spec.ts (~121 l : sin
- **Dépendances schéma** : AUCUNE. Ne dépend que de colonnes subscription.entity déjà sur main (maxProducts, maxEmployees, status, currentPeriodEnd, plan, storeId) et products.isActive. Ne touche aucune colonne du schéma branche (variant_label, pos_session_id, integration_events, opening_float, suppliers, etc.).
- **Recommandation** : Diff strictement additif et behavior-preserving : extraction d'un helper pur unit-testé + trois specs de verrouillage, la logique de refus/quota reste identique (mêmes ForbiddenException, mêmes seuils). Rejouable sur main sans conflit façon option A. Zone facturation/abonnement, hors périmètre NF525 (pas de ventes/avoirs/reçus/session caisse/chaîne d'audit fiscale/Z-report).

## À abandonner (ABANDON) — main couvre déjà

### `customers`  — effort S
- **Rôle** : Référentiel client / fidélité Wesley Club : CRUD client, génération QR de carte, envoi et vérification d'un OTP 6 chiffres (out-of-band SMS/email) pour valider un compte client. Ne produit aucun ticket, vente, avoir ou écriture de journal fiscal.
- **Maturité** : COMPLET — Code compile, routes câblées (create, list, verifyOtp), specs présentes et cohérentes avec le comportement de la branche. MAIS la version session est une régression fonctionnelle/sécurité par rapport à main (voir schemaDeps/rationale), pas une immaturité de code.
- **Tests** : 3 fichiers de spec : customers.service.spec.ts, customers.otp.spec.ts, + nouveau otp-policy.spec.ts (6 cas purs : format, TTL/expiry, cap tentatives, égalité code). Attention : la session a SUPPRIMÉ le bloc de 4 tests 'a
- **Dépendances schéma** : AUCUNE dépendance de schéma DB divergente : CustomerEntity identique sur les deux branches (colonnes anonymized_at / deleted_at présentes des deux côtés, simplement inutilisées côté session). En revanche dépendance de CODE nouvelle absente de main : import de ../../common/pagination (helper totalPag
- **Recommandation** : main est strictement plus avancé et plus sûr que la branche session sur ce module. Le diff session RÉGRESSE deux durcissements présents sur main : (1) il ré-expose l'OTP dans la réponse API (create() retourne otpCode), annulant la sécurité M301/D12 'OTP jamais renvoyé, livraison out-of-band' ; (2) il supprime entièrement l'endpoint d'effacement RGPD POST :id/anonymize (M302, admin-only, audité) et

### `suppliers`  — effort M
- **Rôle** : Référentiel fournisseur tenant-scoped (P327, "variantes option A") : CRUD REST /suppliers (list/create/update/deactivate), nom unique par magasin, soft-delete conservant supplier_id sur les produits, mutations tracées dans l'audit append-only (non bloquant).
- **Maturité** : COMPLET — Controller (4 routes, gardes JWT + Roles manager/admin en écriture), service CRUD complet, module câblé dans app.module.ts (import L54, déclaration L144), DTO create/update validés (class-validator), entité SupplierEntity + migration 1727 dédiée. Rien de stubé/TODO. Cohérent et branché de bout en bo
- **Tests** : 1 spec réelle sur SQL (suppliers.service.pgmem.spec.ts, pg-mem), 5 cas : création + refus doublon même magasin (409) + même nom autorisé dans un autre magasin ; list tenant-scoped active-only + soft-delete préservant la 
- **Dépendances schéma** : Dépend du SCHÉMA BRANCHE, incompatible avec main : (1) suppliers.contact (main = email + phone + country séparés) ; (2) products.variant_label texte (absent sur main) ; (3) products.brand colonne texte déclarative (main = brand_id uuid FK vers table brands normalisée) ; (4) migration 1727-AddProduct
- **Recommandation** : Feature déjà livrée sur main autrement (suppliers + variantes) avec un schéma plus normalisé (table brands, supplier email/phone/country, migrations 1738/1740). L'implémentation session (option A : contact unique, variant_label texte, brand texte, migration 1727, module CRUD) est redondante et schéma-incompatible : non rejouable additivement, le service casserait sur main (colonne contact inexista

### `stock`  — effort L · 🔴 fiscal
- **Rôle** : Gestion des stocks POS : décrément à la vente, ajustement manuel (delta/absolu, lock pessimiste + audit), seuils d'alerte/critique et endpoint /alerts. La branche session y greffe en plus : seuil relatif 20% (baseline par/max), émission d'événements outbox stock.movement/low/depleted, journal append-only stock_movements, et deux endpoints supervision read-only (/reconcile, /variance).
- **Maturité** : COMPLET — Code cohérent routes+service+helpers purs, 9 fichiers spec côté branche (~50 tests : stock-level 9, stock-events 7, stock-signals 7, stock-variance 5, stock.service 5, pgmem 4+4+3+6). Wiring Nest complet (module importe IntegrationModule/StoreOrgResolver/IntegrationEventEntity). Rien de stub/TODO. M
- **Tests** : Riche côté branche : ~50 tests répartis sur 9 specs (helpers purs stock-level/events/signals/variance en unitaire ; stock-movement-journal, stock-reconcile et stock.service en pgmem contre vrai SQL). Couvre seuils, cross
- **Dépendances schéma** : BLOQUANTES sur main : (1) products.stock_baseline_quantity (migration 1721 AddStockBaseline jamais adoptée sur main ; l'entité product de main ne l'a pas) — utilisé dans getAlerts (COALESCE/CEIL) et effectiveAlertThreshold. (2) Table integration_events + IntegrationEventEntity + module integration +
- **Recommandation** : Non rejouable additivement et redondant avec une lignée main plus mûre. (1) Dépend de stock_baseline_quantity et de tout le sous-système outbox integration_events, absents de main qui a divergé vers integration_requests + entités stock persistées (stock-balance/movement/variance, migrations 1735-1758) — les mêmes préoccupations (journal mouvements, variance, signaux stock) sont déjà réimplémentées

### `stock-locations`  — effort S
- **Rôle** : Gestion de stock multi-emplacements (central/magasin/transit/perte) : soldes par emplacement (stock_balances), journal opérationnel des mouvements (stock_movements), réceptions fournisseur, transferts, dispatch central→magasins, pertes, et synchronisation du champ legacy products.stock_quantity. Journal de stock OPÉRATIONNEL, distinct du journal fiscal.
- **Maturité** : COMPLET — Sur la branche session le module est cohérent (controller + service + module + 2 fichiers de specs, tsc clean d'après commit 27d6f0d). MAIS c'est une lignée ANTÉRIEURE : main a divergé en avant avec 4 commits substantiels absents de la session (dea5e25 audit mouvements D20, 00968bc fix sécurité/IDOR
- **Tests** : Session : 14 tests — dispatch-policy.spec.ts (6, helpers purs) + stock-locations.service.spec.ts (8, specs DI-mockées P252-254). Couvrent quantité positive, somme dispatch, stock suffisant, chemins service. À noter : mai
- **Dépendances schéma** : AUCUNE divergence de schéma. Touche uniquement products.stock_quantity (colonne legacy), stock_balances et stock_movements — tables/colonnes standard déjà présentes sur main. Aucune référence à variant_label, pos_session_id, price_override, integration_events, ni colonnes suppliers.
- **Recommandation** : La version session est OBSOLÈTE/superseded : main est déjà en avance de 4 commits (fix sécurité IDOR via assertProductOwned, trail d'audit applicatif D20, diagnostic divergence M107, pertes). Rejouer la session sur main SUPPRIMERAIT ces acquis (régression sécurité + traçabilité). Le seul apport net-new est dispatch-policy.ts (3 fonctions pures triviales : isPositiveQuantity/sumDispatchQuantities/h

### `stores`  — effort S
- **Rôle** : Référentiel magasins multi-tenant : CRUD magasins, hiérarchie org/unité, sync bidirectionnelle TimeWin24 (POS = source de vérité), endpoints d'accès (accessible/me), et agrégation read-only "network-summary" (CA/ventes/ticket moyen/classements réseau) pour le tour de contrôle backoffice.
- **Maturité** : COMPLET — Module entièrement câblé (controller 14 routes gardées par Roles, service, module, mapper) avec specs cohérentes. La version session ajoute même network-stats.ts + sa spec (helpers purs testés). Rien de stub/TODO. Mais "complet" = complet techniquement ; sur le fond la version session est EN RETARD 
- **Tests** : 2 specs. stores.service.spec.ts (couvre create/duplicate-code/findOne/sync) MAIS la version session SUPPRIME 8 tests (~50 lignes) du bloc "legal identity & operating mode" (SIREN/SIRET, franchise→société, checksum). netw
- **Dépendances schéma** : AUCUNE colonne de schéma branche additionnelle. Au contraire : la version session CESSE d'utiliser les champs d'identité légale (siren/siret/tvaIntracom/operatingMode/operatingCompanyName) que main exploite. Le nouveau network-stats.ts est pur (aucune dépendance schéma). Ne touche ni pos_session_id,
- **Recommandation** : La version session du module N'EST PAS additive : elle est ANTÉRIEURE à deux commits déjà sur main — a128bfd (M203 : garde @Roles('admin') sur GET /api/stores, anti data-leak cross-tenant) et d5e4cd4 (validation identité légale SIREN/SIRET + mode d'exploitation→société). Rejouer le diff session RÉGRESSERAIT main : réouverture de la fuite M203 et suppression de la validation légale + ses 8 tests. L

### `stripe-terminal`  — effort S · 🔴 fiscal
- **Rôle** : Client Stripe Terminal (paiement carte présent / card-present) : connection-token, PaymentIntent create/get/cancel avec isolation par magasin, gestion des locations et readers WisePad. Pur client API Stripe, ne persiste rien en base.
- **Maturité** : COMPLET — 8 routes contrôleur (connection-token, payment-intent CRUD, locations, readers list/register), service avec garde assertStripe + contrôle cross-store sur PI, module NestJS exporté. Code cohérent et complet — mais c'est une variante EN RETARD sur main (voir schemaDeps/rationale).
- **Tests** : stripe-terminal.service.spec.ts = 4 tests (garde Stripe non configuré, clé idempotence déterministe, rejet accès cross-store, lecture PI du magasin propriétaire) + payment-intent-key.spec.ts = 5 tests (sha256 documenté, 
- **Dépendances schéma** : AUCUNE dépendance schéma DB (aucun repository TypeORM injecté ; pur client Stripe). ATTENTION : la divergence n'est pas schéma mais fonctionnelle — la branche session SUPPRIME, par rapport à main, tout le garde-fou de capabilité paiement carte (backend GET /stripe-terminal/status + méthode isConfigu
- **Recommandation** : Le module existe déjà sur main dans un état PLUS complet et fiscalement plus sûr. La divergence session est essentiellement une RÉGRESSION : elle retire le gating « fail-closed » du bouton carte ratifié (PR #25) — endpoint GET /stripe-terminal/status + isConfigured() côté backend, service cardPaymentMode.ts côté POS — qui empêche en production toute vente carte fictivement « payée » sans Stripe co

