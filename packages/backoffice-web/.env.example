# Back-office (Vite) — variables d'environnement front.
# Placeholders uniquement — jamais de vraie valeur commitée.

# Base URL de l'API backend (ex: http://localhost:3001/api en dev).
VITE_API_URL=http://localhost:3001/api
